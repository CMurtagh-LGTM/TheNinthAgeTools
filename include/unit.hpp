#pragma once

#include <string>
#include "reactive.hpp"

enum class Rarity {
    COMMON
};

class Unit {
public:
    
private:
    reactive::Number discipline;
    // std::string name;
    // int model_count;
    // Rarity rarity;
    // std::string base;
    // int height;
    // int cha;
    // int mob;
    // int dis;
};
